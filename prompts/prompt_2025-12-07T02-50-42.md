# Pho Restaurant dApp - Contract Customization

## Current Contract Configuration

**Network**: Devnet
**Package ID**: `0x07f7d000c0edd43232395a19b38425d7ec1bcc9b929b3b7ad6ad0d694b441116`
**Module**: pho_restaurant::pho

## Current Move Contract

```move
module pho_restaurant::pho {
    use std::bcs;

    public struct Pho has store {
        broth_quality: u16,
        noodle_thickness: u16, 
        beef_brisket: u16,
        basil: u16,
        cilantro: u16,
        mint: u16,
        star_anise: u16,
        cinnamon: u16,
    }

    public struct PhoBox has key, store {
        id: UID,
        pho: Pho,
    }

    public struct Flag has key, store {
        id: UID,
        user: address
    }

    const EPerfectPho: u64 = 0;

    #[allow(lint(self_transfer))]
    public fun cook_pho(broth_quality: u16, noodle_thickness: u16, beef_brisket: u16, basil: u16, cilantro: u16, mint: u16, star_anise: u16, cinnamon: u16, ctx: &mut tx_context::TxContext) {
        let sender = tx_context::sender(ctx);

        let p = Pho {
            broth_quality,
            noodle_thickness,
            beef_brisket,
            basil,
            cilantro,
            mint,
            star_anise,
            cinnamon,
        };
    
        transfer::public_transfer(PhoBox { id: object::new(ctx), pho: p }, sender);
    }

    #[allow(lint(self_transfer))]
    public fun get_perfect_pho_flag(phobox: &PhoBox, ctx: &mut tx_context::TxContext) {
        // Perfect Northern Pho Recipe: broth=10, noodle=2, beef=70, basil=30, cilantro=25, mint=15, star_anise=2, cinnamon=1
        assert!(bcs::to_bytes(&phobox.pho) == x"0a00020046001e0019000f0201", EPerfectPho);
        transfer::public_transfer(Flag {
            id: object::new(ctx),
            user: tx_context::sender(ctx)
        }, tx_context::sender(ctx));
    }    
}

```

## Request

[Add your customization request here]

---

Please update the contract to:
1. Modify structs as needed
2. Update function signatures
3. Maintain all safety checks
4. Keep proper error handling
